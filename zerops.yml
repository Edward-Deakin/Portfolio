zerops:
  - setup: static
    build:
      base: nodejs@20
      buildCommands:
        - npm i
        - npm run build
      deployFiles:
        - dist/~
      cache:
        - node_modules
        - package-lock.json
    run:
      base: static