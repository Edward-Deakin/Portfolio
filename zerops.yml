zerops:
  - setup: static
    build:
      base: nodejs@22.12.0
      buildCommands:
        - npm i
        - npm run build
      deployFiles:
        - dist/~
      cache:
        - node_modules
        - package-lock.json
    run:
      base: static